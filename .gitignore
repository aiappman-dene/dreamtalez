node_modules/

# Environment files — never commit real values
.env
.env.local
.env.*.local
.env.production
.env.development

# Logs
*.log
server.log
hs_err_pid*.log
replay_pid*.log

# OS artefacts
.DS_Store
Thumbs.db

# IDE / tooling
.claude/

# Duplicate/malformed filenames
*.png.png

# ── Android signing — NEVER commit these ──────────────────────────────────
*.jks
*.keystore
keystore.properties

# ── Firebase runtime configs — NEVER commit these ─────────────────────────
google-services.json
GoogleService-Info.plist

# ── iOS signing ────────────────────────────────────────────────────────────
*.p12
*.mobileprovision
*.cer

# ── Build outputs ──────────────────────────────────────────────────────────
dist/
build/
